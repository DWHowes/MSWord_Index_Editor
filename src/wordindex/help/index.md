# Word Index Editor

For building an **embedded index** in a Microsoft Word manuscript: the kind a
publisher asks for when the index has to travel inside the document rather
than arrive as a separate list of page numbers.

It marks entries as Word `XE` fields, which is what Word's own `INDEX` field
collects at layout. **The index itself is generated by the publisher**, when
the book is composed and the pages exist.

## What it does

- Opens a `.docx` and shows the manuscript as text to read, not as a page
  to look at.
- Shows every index entry the document already has, and where each one sits.
- Creates, edits and deletes entries, with Word's per-level sort keys.
- Handles a book that arrives as several files, in an order you set.
- Checks the finished index for the mistakes that are easy to make and hard
  to see.

## What it does not do

- **It does not decide what to index.** No suggestions, no concordance, no
  model. That is the work.
- **It does not change the manuscript.** What goes back to the publisher
  differs from what arrived by the added fields and nothing else.
- **It does not generate the index.** Word does that, at layout.
- **There are no page numbers anywhere in it.** There are no pages until the
  book is composed.

Start with [Opening a manuscript](02-opening-a-manuscript.md).
